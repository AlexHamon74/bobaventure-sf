{% extends 'base.html.twig' %}

{% block title %}Bob-aventure - Email envoyé{% endblock %}

{% block body %}
    {% include 'assets/header.html.twig' %}
<main class="bgc-linear-primary">
    <section class="d-flex align-items-center justify-content-center py-5">
        <div class="container container-auth bgc-secondary p-5 shadow mx-4">

            <h1>Email envoyé 📩</h1>
            <p>Si un compte correspond à votre adresse email, un message vient d'être envoyé contenant un lien de réinitialisation.</p>

            <p>Ce lien expirera dans{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

            <p class="mt-3">
                Vous n'avez rien reçu ?
                <a href="{{ path('app_forgot_password_request') }}">Réessayer</a>
            </p>

        </div>
    </section>
</main>
    {% include 'assets/footer.html.twig' %}
{% endblock %}